# !! This file is intentionally minimal.
# The canonical fly.toml lives at the REPO ROOT (fly.toml)
# because the Docker build context must include packages/core-python
# and packages/sdk-python which are outside this directory.
#
# Deploy from repo root:
#   fly deploy --config fly.toml --remote-only
#
# Fly.io UI settings:
#   Working directory : (leave empty)
#   Config path       : fly.toml
