[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-test-lab-core"
version = "0.1.0"
description = "Core models and judge interfaces for LLM Test Lab"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
